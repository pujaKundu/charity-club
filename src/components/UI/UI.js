import React from "react";
import Donors from "../Donors/Donors";
import Header from "../Header/Header";

const UI = () => {
  return (
    <div>
      <Header></Header>
      <Donors></Donors>
    </div>
  );
};

export default UI;
